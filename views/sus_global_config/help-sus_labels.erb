Node labels that should be taken offline after a job completes.  Nodes with these labels will be marked temporarily offline by the plugin automatically.  This list should be a comma delimited list and labels are matched exactly.
